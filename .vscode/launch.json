{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "webkit",
            "request": "launch",
            "name": "Lancer Safari",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
